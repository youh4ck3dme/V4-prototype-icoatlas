# ⚠️ SECURITY WARNING: Change these values in production!
# Never use default passwords in production environments.

# Database Configuration
DATABASE_URL=postgresql+asyncpg://postgres:CHANGE_THIS_PASSWORD@db:5432/iluminati
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=iluminati

# Generate secure passwords with: openssl rand -base64 32
